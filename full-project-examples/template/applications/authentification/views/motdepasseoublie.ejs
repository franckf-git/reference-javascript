<% include ./../../views/header %>

<section class="section">
  <div class="container has-text-centered">
    <div class="columns is-centered">
      <div class="column is-5 is-4-desktop">
        <% include ./../../views/messages/erreurs %>
        <form action="/motdepasseoublie" method="post">
          <div class="field">
            <div class="control">
              <input class="input" type="email" name="email" placeholder="Email" value=<% if ( typeof email != 'undefined' ) { %> "<%= email %>" <% } else { %> "" <% } %> required>
                </div>
            </div>
            <div class="field">
              <button class="button is-primary is-fullwidth">M'envoyer un lien pour recréer mon mot de passe</button>
            </div>
        </form>
      </div>
    </div>
  </div>
</section>

<% include ./../../views/footer %>
